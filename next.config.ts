import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // We can add configurations here if needed, like static export or image domain setup
};

export default nextConfig;
